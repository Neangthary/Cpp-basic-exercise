#include <iostream>
using namespace std;

// Write a program to find the volume of cube

int main()
{
    double a, vcube;
    cout << "Enter the value of the connerz: ";
    cin >> a;
    vcube = a * a * a;
    cout << "The volume of cube is: " << vcube << endl;
}